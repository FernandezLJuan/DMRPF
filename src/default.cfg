application:{
    window:{
        title = "simulator"
        size = {w = 900, h = 800}
    }
    environment:{
        cellDims = {w = 30, h = 30}
        gridDims = {rows = 8, cols = 8}
        origin = {x = 300, y = 100}
        obsProb = 0.0;
        n_robots = 2

        map = "paper.map" # placeholder name for map name
    }
}