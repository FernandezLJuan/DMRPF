application:{
    window:{
        title = "simulator"
        size = {w = 900, h = 800}
    }
    environment:{
        cellDims = {w = 30, h = 30}
        gridDims = {rows = 20, cols = 20}
        origin = {x = 200, y = 100}
        obsProb = 0.2;
        n_robots = 3

        map = "algo.map" # placeholder name for map name
    }
}