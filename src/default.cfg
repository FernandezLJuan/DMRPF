application:{
    window:{
        title = "simulator"
        size = {w = 900, h = 800}
    }
    environment:{
        cellDims = {w = 30, h = 30}
        gridDims = {rows = 8, cols = 8}
        origin = {x = 300, y = 100}
        obsProb = 0.0;
        n_robots = 10

        map = "algo.map" # placeholder name for map name
    }
}